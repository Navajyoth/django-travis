language: python
python:
    - "2.7"
env:
    - DJANGO_VERSION=1.8

# command to install dependencies
install:
    - pip install .
    - pip install -r requirements.txt
# command to run tests
script: python manage.py apps.utils test --settings=settings.dev
 
